import React from 'react'
import "./addproducts.css"

const AddProducts = ( props ) => {
  return (
    <div className={`${props.prop ? "absolute w-full h-full back" : "hidden"}`}>
      ddd
    </div>
  )
}

export default AddProducts
